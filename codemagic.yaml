workflows:
  ios-build:
    name: iOS Build
    environment:
      vars:
        XCODE_PROJECT: "iosApp/iosApp.xcodeproj"
        XCODE_SCHEME: "iosApp"
      xcode: latest
      cocoapods: default
      java: 17
    scripts:
      - name: Set up local properties
        script: |
          echo "sdk.dir=$HOME/Library/Android/sdk" > local.properties
      - name: Build iOS
        script: |
          cd iosApp
          xcodebuild build -project iosApp.xcodeproj -scheme iosApp -sdk iphonesimulator -destination 'platform=iOS Simulator,name=iPhone 15,OS=latest' -derivedDataPath ../build_output CODE_SIGNING_ALLOWED=NO | xcpretty
      - name: Package App for Appetize
        script: |
          cd build_output/Build/Products/*-iphonesimulator
          zip -r blitzmath.app.zip *.app
    artifacts:
      - build_output/Build/Products/*-iphonesimulator/blitzmath.app.zip
